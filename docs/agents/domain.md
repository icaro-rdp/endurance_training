# Domain docs

This repository uses a single domain context.

Before exploring or changing the system:

- Read root `CONTEXT.md`.
- Read relevant decisions under `docs/adr/`, when present.
- Use the canonical vocabulary defined in `CONTEXT.md`.
- Surface conflicts with existing ADRs rather than silently overriding them.

Missing ADR directories or documents are created lazily only when a qualifying decision is made.
